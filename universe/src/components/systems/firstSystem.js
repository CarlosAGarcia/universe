import React, { useState } from 'react'
import PropTypes from 'prop-types'

function firstSystem(props) {
    const { systemName } = props

    return (
        <div>
            
        </div>
    )
}

export default firstSystem

firstSystem.propTypes = {
    systemName: PropTypes.string,
}
